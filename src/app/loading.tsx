import { AppBootSplash } from "@/components/AppBootSplash";

export default function Loading() {
  return (
    <div className="app-shell-height" style={{ backgroundColor: "#0a0a0a" }}>
      <AppBootSplash />
    </div>
  );
}
